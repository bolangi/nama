
my @methods = qw(

take_gui
track_gui
refresh
flash_ready
update_master_version_button
paint_button
refresh_oids
paint_button
session_label_configure
length_display
clock_display
manifest
global_version_buttons
destroy_widgets

);
#print "@methods";
#print "hello";
my $text = join '', <>; 
map{	
	my $replacement =  "\$ui->$_";
	#print $replacement, $/;
	$text =~ s/$_/$replacement/;
	} @methods;
print $text;

